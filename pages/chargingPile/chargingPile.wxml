<view class="content">
  <block wx:for="{{listArr}}" wx:key="key"  >
    <chargingPile_detailView  datas="{{item}}" > </chargingPile_detailView>
  </block>
</view>
<loadView isOnLoad="{{onLoad}}"></loadView>

