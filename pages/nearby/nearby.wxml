<view class="content" >
  <block wx:for="{{listArr}}" wx:key="key"  >
    <nearbyView  datas="{{item}}" bindtap="get_is_auth" > </nearbyView>
  </block>
</view>
<loadView isOnLoad="{{onLoad}}"></loadView>

